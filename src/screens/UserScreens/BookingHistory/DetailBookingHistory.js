import {StyleSheet, Text, View} from 'react-native';

const DetailBookingHistory = () => {
  return (
    <View>
      <Text>DetailBookingHistory</Text>
    </View>
  );
};

export default DetailBookingHistory;

const styles = StyleSheet.create({});
